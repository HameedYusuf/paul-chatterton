---
interface Props {
  label?: string;
  heading: string;
  sub?: string;
}
const { label, heading, sub } = Astro.props;
---

<div class="section-heading">
  {label && <p class="label mb-3">{label}</p>}
  <h2>{heading}</h2>
  {sub && <p class="mt-3 max-w-2xl">{sub}</p>}
</div>
